<h2>商品詳細</h2>


  <%= image_tag @item.image %>

  <p>商品名</p>
  <%= @item.name %>

  <p>商品説明</p>
  <%= @item.introduction %>

  <p>ジャンル</p>
  <%= @item.genre_id %>

  <p>税込価格</p><br>
  <p>（税抜価格）</p>
  <%= @item.add_tax_price.to_s(:delimited) %>　(<%= @item.price %>)

  <p>販売ステータス</p>
  <%= @item.is_active? ? "販売中" : "販売停止中" %>

  <%= link_to '編集する', admin_item_path(item) %>

<% end %>